/**
 * @bettersync/pg-adapter
 *
 * Raw node-postgres (pg) adapter for bettersync.
 * Uses parameterized SQL queries — same Postgres dialect as PGlite adapter.
 */
export { pgAdapter } from './pg'
export type { PgAdapterOptions } from './pg'
