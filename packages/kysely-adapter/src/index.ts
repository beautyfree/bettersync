/**
 * @bettersync/kysely-adapter
 *
 * Kysely adapter for bettersync (Postgres).
 * Uses Kysely's CompiledQuery.raw() for type-safe parameterized queries.
 */
export { kyselyAdapter } from './kysely'
export type { KyselyAdapterOptions } from './kysely'
