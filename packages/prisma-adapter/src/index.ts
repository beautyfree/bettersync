/**
 * @bettersync/prisma-adapter
 *
 * Prisma adapter for bettersync (Postgres).
 * Uses $queryRawUnsafe for all operations — works with any Prisma schema.
 */
export { prismaAdapter } from './prisma'
export type { PrismaAdapterOptions } from './prisma'
